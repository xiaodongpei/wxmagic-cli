import wx, { Component, PropTypes } from 'labrador';

class Title extends Component {
  //当前组件的props参数数据类型的定义
  static propTypes = {};
  //当前组件默认的各项参数值
  static defaultProps = {};

  //构造
  constructor(props) {
    super(props);
    this.state = {};
  }

  //当前组件中的子组件集合
  children() {
    return {};
  }
}

export default Title;
